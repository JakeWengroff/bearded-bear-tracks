<h1>All Topics</h1>
<% @topics.each do |topic| %>
  <p><%= link_to topic.title, topic %></p>
<% end %>

<%= link_to "New Topic", new_topic_path(@topic) %>

<%= link_to "Delete Topic", @topic, method: :delete, data: { confirm: 'Are you sure you want to delete this topic?' } %> 


