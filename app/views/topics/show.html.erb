<h1><%= @topic.title %></h1>

<%= link_to "Edit Topic", edit_topic_path(@topic), class: 'button' %>

<%= link_to "New Topic", new_topic_path(@topic), class: 'button' %>

<%= link_to "Delete Topic", @topic, method: :delete, data: { confirm: 'Are you sure you want to delete this topic?' }, class: 'button' %> 


