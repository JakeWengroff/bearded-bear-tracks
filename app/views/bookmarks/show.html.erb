<h1><%= @bookmark.url %></h1>

<%= link_to "Edit Bookmark", edit_user_bookmark_path(bookmark), class: 'button' %>

<%= link_to "New Bookmark", new_user_bookmark_path(bookmark), class: 'button' %>

<%= link_to "Delete Bookmark", @bookmark, method: :delete, data: { confirm: 'Are you sure you want to delete this bookmark?' }, class: 'button' %> 


